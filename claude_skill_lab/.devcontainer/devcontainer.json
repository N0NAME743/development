{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu24.04",

  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "18"
    }
  },

  "mounts": [
    "source=${localEnv:USERPROFILE}/Documents/development/Project_YAKUMO,target=/workspaces/project-yakumo,type=bind"
  ],

  "postCreateCommand": "cd /tmp && echo 'Installing Claude Code... this may take several minutes.' && curl -fsSL https://claude.ai/install.sh | bash && echo 'Claude Code installation complete.' && sed -i 's/\\r$//' /workspaces/development/claude_skill_lab/tools/setup-dashboard-alias.sh && bash /workspaces/development/claude_skill_lab/tools/setup-dashboard-alias.sh"
}